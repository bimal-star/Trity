/** One-line banner so a fresh terminal shows the real dev URL (default Turbopack :3100). */
console.log('[trity] Dev → http://localhost:3100 (Turbopack). If you only see :3000, scroll up for a new run or use a new terminal tab.');
